import { View, Text } from "react-native";

const Layout = () => {
  return (
    <>
      <View style={{ padding: 20 }}>
        <Text style={{ color: "red", textAlign: "center" }}>
          React Native test (Created by Muhammad Malahim)
        </Text>
      </View>
    </>
  );
};

export default Layout;
